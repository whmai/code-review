---
name: code-review
description: "代码审查、后端开发、架构重构、可维护性与工程质量：审查并改进复用、代码放置、模块接口、性能实现、安全编码、注释、日志、常量和枚举；不替代业务 UI/API 验收测试。"
---

# Code Review

用于开发前设计、实现中检查和变更后的代码审查。目标是使实现符合项目既有惯例，并具备长期维护、扩展、诊断与演进能力；业务流程、UI/API/数据结果验收使用 `business-verify`。

## 子代理优先分工

只要当前环境有可用子代理，主代理必须先分工：代码库探索与复用/文件放置审查、数据访问与安全审查、模块接口/可维护性/可观测性审查可分别委派。每个子代理必须负责独立的审查面和证据，不能并发修改同一文件，也不能互相宣布通过；主代理汇总发现、安排修正并完成最终回归审查。

仅当没有可用子代理、无法安全委派，或审查步骤存在不可拆分的强依赖时，当前 AI 才可以独自执行，并在审查记录中说明单代理执行原因。子代理不能绕过用户授权、项目规范或最终审查门禁。

## 先理解现有代码

1. 阅读项目指令、架构说明与相关模块规范。优先使用代码图谱、调用链和精确代码片段发现相近功能；索引不足时才文本搜索。
2. 查找相近业务实现、公共工具、领域类型、模块接口、调用者、错误处理、日志格式和文件放置惯例。
3. 在 [审查模板](references/review-template.md) 中记录候选复用、采用/拒绝理由、风险和可验证门槛。不能因为缺少先验知识就默认新建方法或文件。

## 审查门禁

- 对代码放置、复用、模块接口、模式、注释、日志、稳定值和业务选项，阅读 [后端设计与可维护性门禁](references/backend-design-gate.md)。
- 对数据访问性能、安全编码、数据一致性和外部依赖，阅读 [数据访问与安全门禁](references/data-access-security-gate.md)。
- 项目既有格式化、静态检查、类型检查、构建和测试是最低门槛；不得以无意义 mock、扩大容差或删除测试掩盖问题。

## 修正闭环与输出

发现 P0/P1 或阻碍可维护性的缺陷时，在用户授权的变更范围内自行修正，先复查直接影响，再运行受影响检查和项目要求的验证。持续“审查 → 修正 → 回归审查”，直到所有适用门禁通过。需求歧义、权限/环境缺失或超出授权范围时，保留证据并报告阻塞，不能宣称通过。

输出包含：审查范围、已参照的项目惯例、候选复用与决策、文件放置与模块接口理由、发现及优先级、已执行的修复与验证、未解决风险。后端功能开发应与 `business-verify` 组合使用：本 Skill 审查实现质量，后者验证业务结果。
